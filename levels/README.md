# Levels

The Windows package places the original level here as `Original.dat`.

Add other Jump 'n Bump `.dat` level files to this directory. Subdirectories are
allowed. The launcher discovers them recursively and shows relative paths so
duplicate filenames can still be distinguished.

